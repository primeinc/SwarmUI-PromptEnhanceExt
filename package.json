{
  "name": "swarmui-promptenhance",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for the PromptEnhance SwarmUI extension. The extension itself is a .NET project; this manifest exists only to declare the jsdom dev-dependency and the frontend behavior-test runner.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "test:frontend": "node Tests/frontend/promptenhance.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
